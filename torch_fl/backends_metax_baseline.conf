# MetaX baseline (Top 10 = metax) — Qwen3 opt experiment
# Base: backends_metax.conf

mm = metax
mm.out = metax
_local_scalar_dense = metax
_to_copy = metax
bmm = metax
bmm.out = metax
cat = metax
embedding = metax
add.Tensor = metax
mul.Tensor = metax
silu = metax
rsqrt = metax
mean.dim = metax
cos = metax
sin = metax
neg = metax
pow.Tensor_Scalar = metax
all = metax
_softmax = metax
bitwise_and.Tensor = metax
le.Tensor = metax
where.self = metax
index.Tensor = metax
new_ones = metax
scalar_tensor = metax
ones_like = metax
zeros = metax
silu_backward = metax
sum.dim_IntList = metax
slice_backward = metax
constant_pad_nd = metax
embedding_dense_backward = metax
nll_loss_forward = metax
nll_loss_backward = metax

# Greedy sampling + common framework ops (CUDA .cc + DeviceBoxingGuard on MetaX)
argmax = cuda
argmin = cuda
fill_.Scalar = cuda
arange = cuda
arange.start_step = cuda
sub.Tensor = cuda
eq.Tensor = cuda
eq.Scalar = cuda
max = cuda
bitwise_not = cuda
bitwise_or.Tensor = cuda
any = cuda
any.dim = cuda
lt.Tensor = cuda
lt.Scalar = cuda
cumsum = cuda
