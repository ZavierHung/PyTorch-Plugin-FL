# MetaX v2 (Top 10 = metax_v2) — CUDA-style structure, native metax kernels
# Other ops: metax baseline + CUDA DeviceBoxingGuard for greedy decode

mm = metax_v2
mm.out = metax_v2
_to_copy = metax_v2
bmm = metax_v2
bmm.out = metax_v2
cat = metax_v2
add.Tensor = metax_v2
mul.Tensor = metax_v2
mean.dim = metax_v2
pow.Tensor_Scalar = metax_v2
rsqrt = metax_v2

_local_scalar_dense = metax
embedding = metax
silu = metax
cos = metax
sin = metax
neg = metax
all = metax
_softmax = metax
bitwise_and.Tensor = metax
le.Tensor = metax
where.self = metax
index.Tensor = metax
new_ones = metax
scalar_tensor = metax
ones_like = metax
zeros = metax
silu_backward = metax
sum.dim_IntList = metax
slice_backward = metax
constant_pad_nd = metax
embedding_dense_backward = metax
nll_loss_forward = metax
nll_loss_backward = metax

argmax = cuda
argmin = cuda
fill_.Scalar = cuda
arange = cuda
arange.start_step = cuda
sub.Tensor = cuda
eq.Tensor = cuda
eq.Scalar = cuda
max = cuda
bitwise_not = cuda
bitwise_or.Tensor = cuda
any = cuda
any.dim = cuda
lt.Tensor = cuda
lt.Scalar = cuda
cumsum = cuda
