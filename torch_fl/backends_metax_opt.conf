# MetaX v1 opt (Top 10 = metax_opt) — tune existing metax kernels / copy path
# Other ops: metax baseline + CUDA DeviceBoxingGuard for greedy decode

mm = metax_opt
mm.out = metax_opt
_to_copy = metax_opt
bmm = metax_opt
bmm.out = metax_opt
cat = metax_opt
add.Tensor = metax_opt
mul.Tensor = metax_opt
mean.dim = metax_opt
pow.Tensor_Scalar = metax_opt
rsqrt = metax_opt

_local_scalar_dense = metax
embedding = metax
silu = metax
cos = metax
sin = metax
neg = metax
all = metax
_softmax = metax
bitwise_and.Tensor = metax
le.Tensor = metax
where.self = metax
index.Tensor = metax
new_ones = metax
scalar_tensor = metax
ones_like = metax
zeros = metax
silu_backward = metax
sum.dim_IntList = metax
slice_backward = metax
constant_pad_nd = metax
embedding_dense_backward = metax
nll_loss_forward = metax
nll_loss_backward = metax

argmax = cuda
argmin = cuda
fill_.Scalar = cuda
arange = cuda
arange.start_step = cuda
sub.Tensor = cuda
eq.Tensor = cuda
eq.Scalar = cuda
max = cuda
bitwise_not = cuda
bitwise_or.Tensor = cuda
any = cuda
any.dim = cuda
lt.Tensor = cuda
lt.Scalar = cuda
cumsum = cuda
