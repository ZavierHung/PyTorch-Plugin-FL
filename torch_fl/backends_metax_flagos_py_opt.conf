# Hybrid: Top 10 → metax_opt; other routes same as backends_metax_flagos_py.conf

acos = flagos_python
bitwise_and.Tensor = flagos_python
constant_pad_nd = flagos_python
cos = flagos_python
embedding_dense_backward = flagos_python
index.Tensor = flagos_python
mul.Scalar = flagos_python
nll_loss_forward = flagos_python
nll_loss_backward = flagos_python
silu = flagos_python
silu_backward = flagos_python
sin = flagos_python
slice_backward = flagos_python
where.self = flagos_python

abs = metax
le.Tensor = metax
all = metax

zeros = metax
new_ones = metax
ones_like = metax
scalar_tensor = metax
_local_scalar_dense = metax

mm = metax_opt
mm.out = metax_opt
_to_copy = metax_opt
bmm = metax_opt
bmm.out = metax_opt
cat = metax_opt
add.Tensor = metax_opt
mul.Tensor = metax_opt
mean.dim = metax_opt
pow.Tensor_Scalar = metax_opt
rsqrt = metax_opt

div.Scalar = metax
neg = metax
sum.dim_IntList = metax
embedding = metax
_softmax = metax
sub = metax
relu = metax
sigmoid = metax
clone = metax

argmax = cuda
argmin = cuda
fill_.Scalar = cuda
arange = cuda
arange.start_step = cuda
sub.Tensor = cuda
eq.Tensor = cuda
eq.Scalar = cuda
max = cuda
bitwise_not = cuda
bitwise_or.Tensor = cuda
any = cuda
any.dim = cuda
lt.Tensor = cuda
lt.Scalar = cuda
cumsum = cuda
